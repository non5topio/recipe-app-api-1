
def test_create_user_email_exceeds_max_length(self):
    """Test creating a user with email exceeding maximum length raises an error"""
    email = 'a' * 300 + '@example.com'
    password = 'ValidPass123'
    name = 'Valid Name'
    with self.assertRaises(ValueError) as context:
        get_user_model().objects.create_user(
            email=email,
            password=password,
            name=name,
        )
    self.assertIn('Email exceeds maximum length', str(context.exception))



def test_create_user_email_exceeds_max_length(self):
    """Test creating a user with email exceeding maximum length raises an error"""
    email = 'a' * 300 + '@example.com'
    password = 'ValidPass123'
    name = 'Valid Name'
    with self.assertRaises(ValueError) as context:
        get_user_model().objects.create_user(
            email=email,
            password=password,
            name=name,
        )
    self.assertIn('Email exceeds maximum length', str(context.exception))



def test_create_user_name_exceeds_max_length(self):
    """Test creating a user with name exceeding maximum length raises an error"""
    email = 'user@example.com'
    password = 'ValidPass123'
    name = 'a' * 300
    with self.assertRaises(ValueError) as context:
        get_user_model().objects.create_user(
            email=email,
            password=password,
            name=name,
        )
    self.assertIn('Name exceeds maximum length', str(context.exception))



def test_create_user_email_exceeds_max_length(self):
    """Test creating a user with email exceeding maximum length raises an error"""
    email = 'a' * 300 + '@example.com'
    password = 'ValidPass123'
    name = 'Valid Name'
    with self.assertRaises(ValueError) as context:
        get_user_model().objects.create_user(
            email=email,
            password=password,
            name=name,
        )
    self.assertIn('Email exceeds maximum length', str(context.exception))



def test_create_user_name_exceeds_max_length(self):
    """Test creating a user with name exceeding maximum length raises an error"""
    email = 'user@example.com'
    password = 'ValidPass123'
    name = 'a' * 300
    with self.assertRaises(ValueError) as context:
        get_user_model().objects.create_user(
            email=email,
            password=password,
            name=name,
        )
    self.assertIn('Name exceeds maximum length', str(context.exception))



def test_create_user_invalid_email_format(self):
    """Test creating a user with invalid email format raises an error"""
    email = 'invalidemail.com'
    password = 'ValidPass123'
    name = "Valid Name"
    with self.assertRaises(ValueError) as context:
        get_user_model().objects.create_user(
            email=email,
            password=password,
            name=name,
        )
    self.assertIn('Enter a valid email address', str(context.exception))



def test_create_user_with_exceeding_email_length(self):
    """Test creating a user with an email exceeding 255 characters raises an error"""
    email = 'a' * 246 + '@example.com'  # Exceeds 255 characters
    password = 'ValidPass123'
    name = 'Test User'
    with self.assertRaises(IntegrityError):
        get_user_model().objects.create_user(
            email=email,
            password=password,
            name=name
        )



def test_create_user_with_exceeding_email_length(self):
    """Test creating a user with an email exceeding 255 characters raises an error"""
    email = 'a' * 246 + '@example.com'  # Exceeds 255 characters
    password = 'ValidPass123'
    name = 'Test User'
    with self.assertRaises(IntegrityError):
        get_user_model().objects.create_user(
            email=email,
            password=password,
            name=name
        )



def test_create_user_with_exceeding_name_length(self):
    """Test creating a user with a name exceeding 255 characters raises an error"""
    email = 'test@example.com'
    password = 'ValidPass123'
    name = 'a' * 256  # Exceeds 255 characters
    with self.assertRaises(IntegrityError):
        get_user_model().objects.create_user(
            email=email,
            password=password,
            name=name
        )



def test_create_user_with_exceeding_email_length(self):
    """Test creating a user with an email exceeding 255 characters raises an error"""
    email = 'a' * 246 + '@example.com'  # Exceeds 255 characters
    password = 'ValidPass123'
    name = 'Test User'
    with self.assertRaises(IntegrityError):
        get_user_model().objects.create_user(
            email=email,
            password=password,
            name=name
        )



def test_create_user_with_exceeding_name_length(self):
    """Test creating a user with a name exceeding 255 characters raises an error"""
    email = 'test@example.com'
    password = 'ValidPass123'
    name = 'a' * 256  # Exceeds 255 characters
    with self.assertRaises(IntegrityError):
        get_user_model().objects.create_user(
            email=email,
            password=password,
            name=name
        )



def test_create_user_with_invalid_email_format(self):
    """Test creating a user with an invalid email format raises a ValidationError"""
    email = 'invalid-email-format'
    password = 'ValidPass123'
    name = 'Test User'
    with self.assertRaises(ValidationError):
        get_user_model().objects.create_user(
            email=email,
            password=password,
            name=name
        )



def test_create_user_ignore_is_superuser_extra_fields(self):
    """Test that is_superuser flag is ignored when set via extra_fields in create_user"""
    email = 'attacker@example.com'
    password = 'SecurePass123'
    user = get_user_model().objects.create_user(
        email=email,
        password=password,
        is_superuser=True
    )
    self.assertFalse(user.is_superuser)



def test_create_user_ignore_is_superuser_extra_fields(self):
    """Test that is_superuser flag is ignored when set via extra_fields in create_user"""
    email = 'attacker@example.com'
    password = 'SecurePass123'
    user = get_user_model().objects.create_user(
        email=email,
        password=password,
        is_superuser=True
    )
    self.assertFalse(user.is_superuser)



def test_create_user_with_invalid_email_format(self):
    """Test creating a user with an invalid email format raises a validation error"""
    email = 'invalidemail'
    password = 'ValidPass123'
    name = 'Test User'
    with self.assertRaises(ValidationError):
        get_user_model().objects.create_user(
            email=email,
            password=password,
            name=name
        )



def test_create_user_ignore_is_superuser_extra_fields(self):
    """Test that is_superuser flag is ignored when set via extra_fields in create_user"""
    email = 'attacker@example.com'
    password = 'SecurePass123'
    user = get_user_model().objects.create_user(
        email=email,
        password=password,
        is_superuser=True
    )
    self.assertFalse(user.is_superuser)



def test_create_user_with_invalid_email_format(self):
    """Test creating a user with an invalid email format raises a validation error"""
    email = 'invalidemail'
    password = 'ValidPass123'
    name = 'Test User'
    with self.assertRaises(ValidationError):
        get_user_model().objects.create_user(
            email=email,
            password=password,
            name=name
        )



def test_create_user_with_non_string_email_raises_error(self):
    """Test creating a user with a non-string email raises a TypeError or validation error"""
    email = 12345
    password = 'ValidPass123'
    name = 'Test User'
    with self.assertRaises((TypeError, ValidationError)):
        get_user_model().objects.create_user(
            email=email,
            password=password,
            name=name
        )



def test_create_user_ignore_is_superuser_extra_fields(self):
    """Test that is_superuser flag is ignored when set via extra_fields in create_user"""
    email = 'attacker@example.com'
    password = 'SecurePass123'
    user = get_user_model().objects.create_user(
        email=email,
        password=password,
        is_superuser=True
    )
    self.assertFalse(user.is_superuser)



def test_create_user_with_invalid_email_format(self):
    """Test creating a user with an invalid email format raises a validation error"""
    email = 'invalidemail'
    password = 'ValidPass123'
    name = 'Test User'
    with self.assertRaises(ValidationError):
        get_user_model().objects.create_user(
            email=email,
            password=password,
            name=name
        )



def test_create_user_with_non_string_email_raises_error(self):
    """Test creating a user with a non-string email raises a TypeError or validation error"""
    email = 12345
    password = 'ValidPass123'
    name = 'Test User'
    with self.assertRaises((TypeError, ValidationError)):
        get_user_model().objects.create_user(
            email=email,
            password=password,
            name=name
        )



def test_create_user_with_none_password(self):
    """Test creating a user with password set to None"""
    email = 'nopassword@example.com'
    password = None
    name = 'No Password User'
    user = get_user_model().objects.create_user(
        email=email,
        password=password,
        name=name
    )
    self.assertIsNone(user.password)
    self.assertFalse(user.check_password(''))
    self.assertEqual(user.email, email)
    self.assertEqual(user.name, name)


